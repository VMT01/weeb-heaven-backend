export enum EEnvKey {
    NODE_ENV = 'NODE_ENV',
    PORT = 'PORT',
    API_PREFIX = 'API_PREFIX',
    SWAGGER_PATH = 'SWAGGER_PATH',
}

export enum EValidNodeEnv {
    DEVELOPMENT = 'development',
    BETA_TESTING = 'beta-testing',
    PRODUCTION = 'production',
}
